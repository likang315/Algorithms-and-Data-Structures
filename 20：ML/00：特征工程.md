### 特征工程

------

[TOC]

##### 01：概述

- 将数据转换为能**更好地表示潜在问题的**特征，从而提高机器学习性能。
  - 属性：一般是表格数据的列；
  - 特征：一般只指代对机器学习算法**有益的属性**。

###### 应用领域

- 监督学习【预测分析】
  -  回归：预测**定量数据**；
    - 主要使用均方误差作为测量指标；
  -  分类：预测**定性数据**；
    - 主要使用准确率作为测量指标；
- 无监督学习
  - 聚类：将数据按特征行为进行分类；
    -  主要用轮廓系数作为测量指标
- 统计检验：用相关系数、t 检验、卡方检验，以及其他方法评估并量化原始数据和转换后数据的效果。

###### 机器学习 pipeline

1. 特征理解：学习如何识别定量数据和定性数据。
2. 特征增强：清洗和填充缺失值，最大化数据集的价值。
3. 特征选择：通过统计方法选择一部分特征，以减少数据噪声。
4. 特征构建：构建新的特征，探索特征间的联系。
5. 特征转换：提取数据中的隐藏结构，用数学方法转换数据集、增强效果。
6. 特征学习：利用深度学习的力量，以全新的视角看待数据，从而揭示新的问题，并予以解决。

##### 02：特征理解

```python
salary_ranges = pd.read_csv('../data/Salary_Ranges_by_Job_Classification.csv')
# 查看前几行
salary_ranges.head()
# 查看数据有多少行，是否有缺失值，以及每列的数据类型
salary_ranges.info() 
```

###### 定量数据和定性数据

- 定量数据本质上是**数值**，应该是衡量某样东西的数量。

  - ```python
    # 显示描述性统计
    salary_ranges.describe() 
    ```

- 定性数据本质上是**类别**，应该是描述某样东西的性质。

  - ```python
    # 将工作种类转换为字符串
    salary_ranges['Grade'] = salary_ranges['Grade'].astype(str)
    # 检查转换是否生效
    salary_ranges.info() 
    ```

###### 数据等级【向上升级】

- 定类等级（nominal level）：只按名称分类；

  - 众数

    - ```python
      # 对工作种类进行计数
      salary_ranges['Grade'].value_counts().head() 
      # 对工作种类绘制条形图
      salary_ranges['Grade'].value_counts().sort_values(ascending=False).head(20).plot
      (kind='bar') 
      # 对工作种类绘制饼图（前 5 项）
      salary_ranges['Grade'].value_counts().sort_values(ascending=False).head(5).plot
      (kind='pie') 
      ```

- 定序等级（ordinal level）：可以自然排序；

  - 箱线图

    - ```python
      # 定序等级也可以画箱线图
      art_ratings.value_counts().plot(kind='box') 
      ```

- 定距等级（interval level）：不仅可以对值进行排序和比较，而且可以加减；

  - 均值

    - ```python
      # 直方图：连续温度的分布
      climate['AverageTemperature'].hist() 
      # 通过年份字段，生成不同年的直方图
      climate_sub_us['AverageTemperature'].hist(by=climate_sub_us['year'],
       sharex=True, sharey=True,
       figsize=(10, 10),
       bins=2
      ```

  - 标准差

    - ```python
      # 移动平均，滑动窗口，用前 10 年的值求出均值滑动
      climate_sub_us.groupby('year').mean(['AverageTemperature']
                                          .rolling(10).mean().plot()
      ```

- 定比等级（ratio level）：绝对零点的概念，可以做乘除运算；

  - 倍数

    - ```python
      # 最高工资和最低工资的比值
      sorted_df = salary_ranges.groupby('Grade')[['Biweekly HighRate']]
      .mean().sort_values('Biweekly High Rate', ascending=False)
      sorted_df.iloc[0][0] / sorted_df.iloc[-1][0] 
      ```

###### 数据处理流程

<img src="https://github.com/likang315/Algorithms-and-Data-Structures/blob/master/20%EF%BC%9AML/photos/Feature-Understanding.png" alt="Feature-Understanding" style="zoom:35%;" />

##### 03：特征增强（数据清洗）

###### 探索性数据分析（识别数据中的缺失值）

1. 观察数据前几行；

   - ```
     pandas.head()
     ```

2. **相关性矩阵**：直接观测出，结果和特征强度关系、特征间的相关性强度

3. 观察数据集结果占比；

   - ```python
     data['result_col'].value_counts(normalize=True)：
     ```

4. 基本描述性统计：看均值，标准差，百分位值；

   - ```python
     data.describe()
     ```

###### 处理缺失值

- 删除有害的行

  - ```
    df.dropna() //删除所有行缺失值的行 NAN
    ```

  - 删除行后，查看变化率，会影响整个数据得形状。

- 填充缺失值

  - 使用均值填充，理解业务用实际值填充。


###### 标准化

- 当输入特征的数值范围（量纲、单位、大小）差异很大时，模型会被“带偏”，很多机器学习算法的性能会显著下降，甚至无法正常工作。

- **Z-score标准化**：特征缩放的一种方式

  - 把原始数据转换成**“距离均值多少个标准差”**的形式，使得新数据的中心在 0，波动单位为 1，从而消除量纲影响，便于比较和建模。

  - 对每个特征值 *x*，标准化后的值为：
    $$
    x_{\text{standardized}} = \frac{x - \mu}{\sigma}
    $$
    其中：

    - *μ*：该特征在**训练集上的均值**
    - *σ*：该特征在**训练集上的标准差**


###### 归一化（Min-Max 归一化）

-  将不同特征的数值范围**统一压缩到一个固定的、小的区间内（通常是 [0, 1] 或 [-1, 1]）**，消除量纲和数值大小差异带来的影响。

- 换一把尺度 0-1 的尺子

  - 公式（缩放到 [0, 1] 区间）：

  - $$
    x_{\text{normalized}} = \frac{x - x_{min}}{x_{max} - x_{min}}
    $$

  - $x$：原始值

  - $x_{min}$：该特征在训练集中的最小值

  - $x_{max}$：该特征在训练集中的最大值

##### 04：特征构键














